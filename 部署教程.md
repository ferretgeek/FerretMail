# Ferret Mail 开源版部署教程

文档状态：2026-08-07 已按当前程序、自动化测试和实际浏览器检查核对。

本文面向 AI 或运维人员。完成后应得到一个由 systemd 托管、可通过 SMTP 25 端口收信、带网页后台与 API 的域名邮箱服务。

## 1. 部署前提

- Ubuntu 22.04/24.04 或 Debian 12。
- Python 3.10+。
- 一个固定公网 IPv4。
- 域名 DNS 托管在 Cloudflare。
- 云厂商允许入站 TCP 25；部分服务商默认封禁邮件端口。
- 防火墙允许 TCP 25；HTTP 直连时还需允许 8710，使用反向代理时允许 80/443。

本程序只负责收件，不提供发信能力。生产后台和 API 推荐配置 HTTPS；若暂时使用 HTTP，Token 会以明文经过网络。

## 2. 准备程序、账号和目录

```bash
python3 --version
sudo useradd --system --home /var/lib/ferret-mail --shell /usr/sbin/nologin ferret-mail 2>/dev/null || true
sudo install -d -o root -g root -m 0755 /opt/ferret-mail
sudo install -d -o ferret-mail -g ferret-mail -m 0750 /var/lib/ferret-mail
sudo install -d -o ferret-mail -g ferret-mail -m 0750 /var/lib/ferret-mail/backups
sudo install -o root -g root -m 0755 ferret_mail_service.py /opt/ferret-mail/ferret_mail_service.py
```

先编译检查：

```bash
python3 -m py_compile /opt/ferret-mail/ferret_mail_service.py
```

在上传前的源码目录还应运行完整回归测试：

```bash
python3 -m unittest -v test_ferret_mail_service.py
```

## 3. 创建环境变量

生成全局管理员 Token：

```bash
python3 -c 'import secrets; print(secrets.token_urlsafe(48))'
```

复制模板并修改：

```bash
sudo install -o root -g root -m 0600 .env.example /etc/ferret-mail.env
sudo nano /etc/ferret-mail.env
```

至少替换：

```env
PANEL_TOKEN=生成的长随机Token
MAIL_DOMAIN=example.com
MAIL_ROOT_DOMAINS=example.com
PUBLIC_IP=服务器公网IPv4
TRUSTED_HOSTS=服务器公网IPv4,127.0.0.1,localhost,example.com,mail.example.com,*.example.com
```

多主域名写入 `MAIL_ROOT_DOMAINS`，额外子域名可写入 `MAIL_EXTRA_DOMAINS`。完整容量、限流、备份和 CORS 参数见 `.env.example`。

## 4. 创建 systemd 服务

创建 `/etc/systemd/system/ferret-mail.service`：

```ini
[Unit]
Description=Ferret Mail Service
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=ferret-mail
Group=ferret-mail
EnvironmentFile=/etc/ferret-mail.env
WorkingDirectory=/opt/ferret-mail
ExecStart=/usr/bin/python3 /opt/ferret-mail/ferret_mail_service.py
Restart=always
RestartSec=3
TimeoutStopSec=35
UMask=0077
LimitNOFILE=4096
TasksMax=256
AmbientCapabilities=CAP_NET_BIND_SERVICE
CapabilityBoundingSet=CAP_NET_BIND_SERVICE
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ProtectKernelTunables=true
ProtectKernelModules=true
ProtectControlGroups=true
ReadWritePaths=/var/lib/ferret-mail

[Install]
WantedBy=multi-user.target
```

启动并检查：

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now ferret-mail.service
sudo systemctl status ferret-mail.service --no-pager
sudo journalctl -u ferret-mail.service -n 100 --no-pager
sudo ss -lntp | grep -E ':25|:8710'
curl -fsS http://127.0.0.1:8710/health
```

如果当前发行版不支持上述某个 systemd 加固项，日志会明确指出；只删除不支持的那一项，不要把服务改回 root 运行。

## 5. 防火墙

直接开放面板端口：

```bash
sudo ufw allow 25/tcp
sudo ufw allow 8710/tcp
sudo ufw reload
```

若使用 HTTPS 反向代理，只对公网开放 25、80、443，并让代理转发到 `127.0.0.1:8710`。此时把 `HTTP_HOST` 改为 `127.0.0.1`，将实际面板域名加入 `TRUSTED_HOSTS`，并设置 `PUBLIC_BASE_URL=https://实际面板域名`。SMTP 使用的 `mail.example.com` 不能走 Cloudflare HTTP 代理。

## 6. 生产 HTTPS 反向代理

长期运行不应把 8710 明文暴露到公网。以 Nginx 为例，证书就绪后可使用：

```nginx
server {
    listen 80;
    server_name panel.example.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name panel.example.com;

    ssl_certificate /etc/letsencrypt/live/panel.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/panel.example.com/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:8710;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Connection "";
        proxy_read_timeout 40s;
        proxy_send_timeout 40s;
    }
}
```

对应环境变量至少包括：

```env
HTTP_HOST=127.0.0.1
TRUSTED_HOSTS=127.0.0.1,localhost,panel.example.com
PUBLIC_BASE_URL=https://panel.example.com
```

证书、80/443 防火墙和 DNS A/AAAA 记录必须按实际域名配置。确认 HTTPS 正常后，移除公网 8710 放行规则。`CORS_ALLOWED_ORIGINS` 默认继续留空；只有确有浏览器跨站调用时才加入完整 Origin。

## 7. Cloudflare DNS

以 `example.com` 为例：

MX 记录：

- Type：`MX`
- Name：`@`
- Mail server：`mail.example.com`
- Priority：`10`

A 记录：

- Type：`A`
- Name：`mail`
- IPv4 address：服务器公网 IPv4
- Proxy status：`DNS only / 灰云`

`mail` 的 A 记录必须是灰云。Cloudflare 的橙云代理不转发普通 SMTP 25 端口。

子域名由后台给出精确的 MX Name 和目标值；不要凭字符串猜记录名称。DNS 更新后可等待数分钟再检查。

## 8. 首次登录与收件验收

访问：

```text
http://YOUR_SERVER_IP:8710/mail
```

输入 `/etc/ferret-mail.env` 中的 `PANEL_TOKEN`。依次完成：

1. 在“运行总览”确认 HTTP、SMTP、数据库、磁盘和备份状态。
2. 运行 DNS 检查，确认 MX 与 A 记录通过。
3. 从外部邮箱发送一封测试邮件到 `a@example.com`。
4. 确认邮件出现在列表，正文可读，验证码可复制，附件可下载。
5. 打开 `http://YOUR_SERVER_IP:8710/api-docs.md`，确认动态 API 文档可下载。

SMTP 收到符合规则、但尚未创建的地址时，会自动创建该别名。

## 9. 域名和独立 Token

- 子域名：在“域名管理与接入”中添加，例如 `team` 会生成 `team.example.com`。
- 其他主域名：使用全局管理员 Token 添加完整域名，再为它单独配置 Cloudflare DNS。
- 独立管理：为主域名或子域名生成域名 Token，使用 `/mail/<完整域名>` 登录。
- 删除域名：按界面输入完整域名和确认词。系统会先创建备份，随后删除该域名树下的别名、邮件、附件、Token 和接码链接。

默认主域名不能删除；其他主域名只能由全局管理员删除。

## 10. API 对接

动态文档：

```text
http://YOUR_SERVER_IP:8710/api-docs.md
http://YOUR_SERVER_IP:8710/usage-guide.md
```

推荐的取件流程：

1. `POST /ui-api/aliases` 创建别名。
2. `GET /ui-api/changes` 长轮询等待变更。
3. `GET /ui-api/messages` 获取列表。
4. `GET /ui-api/message?id=...` 获取完整正文、验证码、链接与附件。

Token 应只存放在受控服务端或密钥系统。公开网页、浏览器扩展和移动端安装包中不要嵌入管理员 Token。公开取件使用单别名接码链接。

## 11. 安全更新与回滚

在上传新文件的目录先检查：

```bash
python3 -m py_compile ferret_mail_service.py
```

生产更新：

```bash
stamp=$(date +%Y%m%d-%H%M%S)
sudo cp -a /opt/ferret-mail/ferret_mail_service.py "/opt/ferret-mail/ferret_mail_service.py.bak-$stamp"
sudo -u ferret-mail cp -a /var/lib/ferret-mail/inbox.sqlite3 "/var/lib/ferret-mail/backups/manual-before-update-$stamp.sqlite3"
sudo install -o root -g root -m 0755 ferret_mail_service.py /opt/ferret-mail/ferret_mail_service.py
sudo systemctl restart ferret-mail.service
sudo systemctl is-active ferret-mail.service
curl -fsS http://127.0.0.1:8710/health
```

若服务启动失败，查看日志并恢复刚才的源码备份：

```bash
sudo journalctl -u ferret-mail.service -n 150 --no-pager
sudo cp -a /opt/ferret-mail/ferret_mail_service.py.bak-时间戳 /opt/ferret-mail/ferret_mail_service.py
sudo systemctl restart ferret-mail.service
```

恢复数据库应优先使用后台的备份恢复功能，并在操作前再次创建当前数据库副本。

## 12. 备份

- 自动备份目录：`BACKUP_DIR`
- 自动备份周期：`AUTO_BACKUP_HOURS`
- 默认最多保留：`MAX_BACKUPS`（当前默认 30）
- 最大允许陈旧时间：`BACKUP_MAX_AGE_HOURS`（超过后 `/health` 降级）
- 删除域名、清空和恢复等高风险操作会按程序逻辑创建保护性备份

备份会先写临时文件、校验后原子落盘。仍必须另行复制到不同服务器或对象存储；同一台服务器上的备份无法应对服务器被误删或磁盘损坏。复制任务不由本程序自动创建。

## 13. 常见故障

### 收不到邮件

依次检查云厂商 25 端口限制、UFW/安全组、`ss -lntp`、Cloudflare MX、`mail` A 记录灰云状态、`PUBLIC_IP` 和 systemd 日志。

### 面板打不开

检查服务是否 active、8710 是否监听、防火墙/反向代理是否放行，以及请求 Host 是否包含在 `TRUSTED_HOSTS`。

### Token 无法登录

确认使用全局管理员 Token 或当前域名对应 Token，域名 Token 未被禁用，并访问正确的 `/mail/<domain>` 页面。

### 返回 429

表示命中限流。对接程序应复用长轮询、使用指数退避并限制并发，不要高频刷新列表。

### 邮件正文颜色异常

当前 UI 会把 HTML 邮件放进隔离的白色阅读画布。若开源副本没有该效果，确认部署的是最新文件，清浏览器缓存后再测试。

## 14. 卸载

1. 先停止公网入口和 `ferret-mail.service`，确认 TCP 25 与 8710 不再由本服务监听。
2. 使用后台或 SQLite 一致备份创建最后一份加密备份，并在隔离目录验证可打开；备份包含邮件、附件引用、Token 和操作记录。
3. 禁用并移除 systemd 单元、反向代理站点、防火墙例外和 DNS MX/A 记录，避免邮件继续投递到已停止的主机。
4. 只有明确放弃恢复后才删除 `/var/lib/ferret-mail`、`/opt/ferret-mail` 与秘密环境文件；删除不会撤回已经复制出去的邮件或 Token。
5. 检查专用系统账号是否仍拥有其他文件后再删除账号。若凭据可能泄露，轮换域名 Token、管理员 Token、Webhook Secret 和受影响的公开取件链接。

Stop SMTP/HTTP traffic and the service before removal. Verify a final encrypted backup, then remove the unit, proxy, firewall, and DNS routes. Delete application/data/secret directories and the service user only after explicitly abandoning restore. Rotate any exposed token or webhook secret; file deletion cannot invalidate copied data.

## 15. 最终验收

- `/health` 返回 `ok: true`。
- TCP 25 和 HTTP 服务均监听。
- systemd 重启后自动恢复。
- DNS 检查通过。
- 外部测试邮件能实际进入列表。
- 邮件详情、验证码、链接和附件可用。
- 自动别名、独立接码链接和 API 长轮询可用。
- 桌面与手机后台无横向溢出，关键文字和按钮清晰可操作。
- `/api-docs.md` 与 `/usage-guide.md` 可下载。
- 已验证备份位置、权限和异机备份方案。
- `HEAD /health` 可用，健康详情显示备份存在且未过期。
- 8710 不直接暴露公网，后台与 API 通过 HTTPS 访问。
