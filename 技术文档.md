# Ferret Mail 开源版技术文档

文档状态：2026-08-07 已按当前开源源码、自动化测试和实际浏览器检查核对。

本文面向后续维护项目的 AI、开发者和运维人员，说明架构、数据、权限、接口、UI、安全边界和验证要求。

## 1. 产品边界

Ferret Mail 是一个单文件 Python 域名邮箱/接码系统，包含：

- SMTP 收信服务。
- HTTP 管理后台和 JSON API。
- 多主域名、子域名与邮箱别名。
- 自动创建收件别名。
- 邮件列表、全文筛选、验证码识别、链接和附件。
- 独立接码链接。
- 长轮询、Webhook、DNS 检查、备份、日志和健康检查。

它只负责收件，不提供 SMTP 发信或发信 API。面板当前可直接通过 HTTP 运行，但生产环境应由反向代理提供 HTTPS。

## 2. 运行架构

入口为 `ferret_mail_service.py`，不需要前端构建，不依赖第三方 Python 包：

- `http.server.ThreadingHTTPServer`：网页、API 和文件下载。
- `asyncio` 与 socket：SMTP 会话。
- `sqlite3`：域名、别名、邮件、附件、统计、日志和迁移状态。
- `email` 标准库：MIME、正文、HTML、附件和头部解析。
- 内嵌 HTML/CSS/JavaScript：管理后台和公开接码页。
- 内嵌 Markdown 生成器：`/api-docs.md`、`/usage-guide.md`。

HTTP 和 SMTP 在同一进程内运行。生产环境由 systemd 管理，数据目录与源码目录分离。

## 3. 启动配置

所有生产差异应通过环境变量注入，不应把真实部署值写进开源源码。完整字段见 `.env.example`，主要类别包括：

- 身份与域名：`PANEL_TOKEN`、`MAIL_DOMAIN`、`MAIL_ROOT_DOMAINS`、`MAIL_EXTRA_DOMAINS`。
- 网络：`PUBLIC_IP`、`HTTP_HOST`、`HTTP_PORT`、`SMTP_HOST`、`SMTP_PORT`。
- 数据：`DB_PATH`、`BACKUP_DIR`、自动备份数量、周期、最大年龄、容量、最低磁盘余量和 SQLite 同步级别。
- 邮件：保留时长、原始邮件、正文和附件大小。
- 配额：默认别名数、邮件数和存储容量。
- 防滥用：HTTP、写操作、长轮询、公开接码、SMTP 连接、收件人和命令限流。
- 安全：`TRUSTED_HOSTS`、`CORS_ALLOWED_ORIGINS`、`PUBLIC_BASE_URL`。
- 资源边界：HTTP 与 SMTP 总连接数、请求/空闲/DATA 超时。
- 运维：操作日志、失败邮件和清理记录保留。

默认保留时长是 72 小时；单封原始邮件默认上限 25 MiB，单附件保存上限 10 MiB，自动备份默认保留 30 份。实际生产值以环境变量和域名设置为准。程序启动时会拒绝空白/占位 Token、非法端口、过低资源边界和错误 Origin 等危险配置。

## 4. 数据模型

SQLite 启动时自动初始化并补列。核心表：

- `mail_domains`：域名类型、父级关系、Token、启停、配额、保留、Webhook 和品牌设置。
- `aliases`：别名、备注、独立接码 token、启停状态、邮件数与最近活动缓存。
- `mails`：发件人、收件人、主题、文本、HTML、原始源码、验证码、链接标记、已读、星标、置顶和存储字节。
- `attachments`：附件元数据和可下载内容。
- `domain_usage`：每个域名的别名数、邮件数和存储字节增量汇总。
- `service_meta`：迁移版本、缓存版本等元信息。
- `operation_logs`：后台操作审计。
- `failed_mails`：投递失败或解析异常。
- `cleanup_runs`：定期清理记录。

统计由触发器和写入路径增量维护，邮件删除触发器保证附件不会成为孤儿数据。列表、配额判断和运行总览不得退回到频繁全表 `COUNT` 或正文扫描。SQLite 使用 WAL 与默认 `FULL` 同步级别；每个连接异常时明确回滚并关闭。

## 5. 收件数据流

SMTP 会话完成 DATA 后：

1. 校验命令顺序、消息大小、收件人数量和目标域名状态。
2. 规范化并去重收件人。
3. 解析一次 MIME、主题、文本、HTML、链接、验证码和附件。
4. 在同一事务内检查每个域名配额。
5. 为有效且不存在的地址自动创建别名。
6. 为所有收件人原子写入邮件；任一关键步骤失败则整体回滚。
7. 更新域名和别名增量统计。
8. 提交后通知长轮询等待者。
9. 异步投递 Webhook，慢 Webhook 不阻塞 SMTP。

SMTP 对配额、格式等永久错误返回 5xx；数据库忙、磁盘或其他暂时性存储错误返回 `451`，使上游邮件服务器按协议重试，避免把临时故障误报成永久拒收。

验证码识别使用多语言上下文评分，支持常见分隔符、全角字符与 Unicode 数字，并降低订单号、手机号和日期误判。筛选使用写入时预计算字段。

## 6. 权限模型

- 全局管理员：`PANEL_TOKEN`。管理全部主域名、子域名、Token、备份、日志和全局运维。
- 主域名管理员：主域名 Token。管理该主域名及其子域名。
- 域名管理员：普通域名 Token。只访问对应域名。
- 公开接码访问者：`alias_...` token。只读取一个别名，不等同后台 Token。

后台接口接受 `Authorization: TOKEN`、`Authorization: Bearer TOKEN` 或 `X-Token: TOKEN`。新接口必须复用现有鉴权、域名归属和邮箱范围校验，不能仅凭域名字符串后缀判断权限。

## 7. 页面与动态文档

- `/mail`：默认主域名后台。
- `/mail/<domain>`：指定完整域名后台。
- `/code/<alias_share_token>`：单别名公开接码页。
- `/api-docs.md`：按当前域名和服务地址动态生成的 API 文档。
- `/usage-guide.md`：按当前域名和服务地址动态生成的使用说明。
- `/health`：无鉴权健康检查。

动态文档是公开下载内容，只能出现占位 Token，禁止加入真实凭据。

## 8. 主要 API

后台 API 统一位于 `/ui-api/*`：

- 鉴权：`GET /ui-api/auth-check`
- 别名：`GET/POST /ui-api/aliases`、`POST /ui-api/bulk-aliases`、`POST /ui-api/delete-alias`
- 邮件：`GET /ui-api/messages`、`GET /ui-api/message`、`GET /ui-api/attachment`
- 监听：`GET /ui-api/changes`
- 状态与批量：`POST /ui-api/message-state`、`POST /ui-api/messages-bulk`
- 清理：`POST /ui-api/delete-message`、`POST /ui-api/clear-messages`、`POST /ui-api/clear-alias-messages`
- 接码链接：`POST /ui-api/alias-share-token`、`POST /ui-api/alias-share-export`
- 域名：`GET/POST /ui-api/domains`、`POST /ui-api/domains-bulk`、`POST /ui-api/domain-settings`、`POST /ui-api/domain-token`、`POST /ui-api/delete-domain`
- DNS 与导出：`GET /ui-api/dns-check`、`GET /ui-api/dns-check-bulk`、`GET /ui-api/domain-export`
- 运维：`GET /ui-api/admin/overview`、`GET /ui-api/admin/health`、审计、失败邮件、清理记录和备份接口

公开接码 API：

- `GET /public-api/alias-share`
- `GET /public-api/alias-share/message`
- `GET /public-api/alias-share/changes`

旧版 `/public/addUser`、`/public/emailList` 仅用于兼容；新集成优先使用 `/ui-api/*`。完整参数和响应以运行时 `/api-docs.md` 为准。

## 9. API 行为约定

- 大多数 JSON 接口返回 `{"code":200,"message":"success","data":...}`。
- 列表 `pageSize` 最大 100。
- `GET /ui-api/message?id=...` 读取详情时会把邮件标为已读。
- `GET /ui-api/changes` 最长等待约 25 秒；`since` 使用毫秒时间戳。
- 邮箱前缀转小写，允许 `[a-z0-9._+-]{1,64}`。
- 请求体默认最大 2 MiB；批量别名最多 10000 个，批量子域名最多 200 个。
- 429 表示限流；客户端应退避并控制并发。
- CORS 只允许 `CORS_ALLOWED_ORIGINS` 中的完整来源。

## 10. Webhook

域名可配置 `webhook_url` 和 `webhook_enabled`。收信成功后异步 POST JSON 摘要，包含事件、域名、邮件 ID、收发件地址、主题、验证码和接收时间。

当前边界：

- 超时约 5 秒。
- 不跟随重定向。
- 没有内置签名。
- 没有自动重试或死信队列。

接收方应使用 HTTPS、随机不可猜 URL、幂等处理和来源网络限制。需要可靠重试时，应在 Ferret Mail 前增加内部队列或网关。

## 11. 备份与高风险操作

删除域名、清空数据和恢复备份必须保留明确确认。删除子域名输入完整域名；删除其他主域名需要全局管理员并再次输入确认词。默认主域名不能删除。

数据库备份先写临时文件、执行 `quick_check`，再原子替换为正式备份，失败不会留下看似可用的半成品。数据库恢复前会创建当前数据库副本，并暂时阻止新数据库请求进入，恢复后再次执行完整性检查。

健康检查会同时判断备份是否存在、是否超过 `BACKUP_MAX_AGE_HOURS`、总容量是否超限，以及数据盘和备份盘是否达到最低余量。程序内备份仍位于同一服务器，生产运维必须另做异机或对象存储备份；同机备份不能抵御整机或磁盘损坏。

## 12. UI 与交互规范

当前后台采用浅色天蓝白 Skyline 视觉，支持深色模式和减少动态效果：

- 内容、正文和表单文字保持清晰，不使用浅灰小字承载关键信息。
- 折叠标题、筛选与批量操作、按钮和触控目标应保持足够高度。
- HTML 邮件显示在隔离的白色阅读画布中，避免邮件自身颜色与后台主题混在一起。
- 桌面是侧栏、邮件列表、详情的高效布局；平板和手机重新排布，不只是等比缩小。
- 长地址、主题、Token、表格和附件不能造成页面横向溢出。
- loading、empty、error、disabled、hover、focus、网络失败、重复提交和危险确认状态必须可见。
- 动画只使用轻量 CSS，并尊重 `prefers-reduced-motion`。

前端是 Python 内嵌字符串。修改 HTML/CSS/JavaScript 后，除 Python 编译外，还要抽取 `<script>` 做 JavaScript 语法检查，并实看 390×844、768×1024、1440×900 及 Windows 100%/125%/150% 缩放。

## 13. 安全边界

- 开源目录不得包含真实 Token、SSH 密码、生产 IP、Cookie、数据库、附件、日志或备份。
- 生产 Token 不应进入公开前端、浏览器扩展、移动端安装包、日志或公开仓库。
- `TRUSTED_HOSTS` 必须覆盖真实入口，但不能无边界放宽。
- CORS 按完整 Origin 精确放行，默认留空。
- 使用反向代理时通过 `PUBLIC_BASE_URL` 固定对外 HTTPS Origin，不信任任意客户端提供的转发协议头。
- HTML 邮件必须继续经过现有清理与隔离，不直接注入后台 DOM。
- 附件下载继续校验邮件和域名权限。
- 不用宽泛异常吞错；失败应写入可定位日志或返回明确错误。
- 不通过关闭限流、鉴权或确认来“修复”调用失败。

## 14. 代码修改原则

- 保持单文件和标准库架构，除非新需求确实无法可靠实现。
- 数据库变更必须兼容旧库并可重复执行。
- 多主域名复用 `root_domain_for`、`domain_in_root` 等现有关系函数。
- `/health` 与 `/ui-api/admin/health` 继续复用同一状态来源。
- 收件写入保持多收件人原子性。
- 删除、恢复和清空操作保持备份与确认链路。
- 不把生产环境默认值带回开源副本。

## 15. 验证

最低检查：

```bash
python3 -m py_compile ferret_mail_service.py
python3 -m unittest -v test_ferret_mail_service.py
```

本地运行示例：

```bash
PANEL_TOKEN=dev-token \
MAIL_DOMAIN=example.com \
MAIL_ROOT_DOMAINS=example.com \
PUBLIC_IP=127.0.0.1 \
DB_PATH=/tmp/ferret-mail.sqlite3 \
BACKUP_DIR=/tmp/ferret-mail-backups \
HTTP_HOST=127.0.0.1 \
HTTP_PORT=8710 \
SMTP_HOST=127.0.0.1 \
SMTP_PORT=2525 \
TRUSTED_HOSTS=127.0.0.1,localhost \
python3 ferret_mail_service.py
```

应验证：

- `/health`、`/mail`、`/api-docs.md`、`/usage-guide.md`。
- 正确/错误 Token 和跨域名权限。
- SMTP 命令顺序、大小限制、多收件人与失败回滚。
- 自动别名、验证码正反例、HTML 正文、附件。
- 列表分页、搜索、长轮询、429 和网络失败。
- 接码链接只可访问一个别名。
- Webhook 超时不阻塞收件。
- 旧库迁移、备份创建、恢复和清理。
- 桌面、平板、手机、键盘焦点、滚动和溢出。
- 错误 JSON、过大请求、错误 Host、未授权 Origin 和 SMTP 临时故障重试码。

上线后还必须从公网真实发送一封测试邮件；仅有编译通过或健康接口成功不代表收件链路完成。

## 16. 脱敏说明

本目录的默认域名为 `example.com`，默认公网 IP 为 `127.0.0.1`。开源包只提供程序、配置模板和文档，不包含任何生产数据或凭据。
